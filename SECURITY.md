# Security and privacy

## 安全模型

Obsidian AutoGrow只应访问用户在本地配置中明确授权的目录。目录发现、语义读取和创作者灵感是不同权限层级，不应互相推导授权。

## 默认原则

- 目录发现默认关闭，且只读取元数据；
- 创作者灵感默认关闭；
- 配置、队列、备份和报告保存在知识库之外；
- 语义读取遵守扩展名、大小和排除规则；
- 笔记写入限制在已配置的Obsidian仓库内；
- 现有笔记只更新AI维护区域；
- 写入冲突时停止，不强行覆盖。

## 已知限制

文件名过滤不能保证发现所有密钥。Codex在读取候选文件前仍需遵守最小读取原则，避免普通配置文件中的敏感值。

云盘同步、Obsidian和自动任务可能并发访问文件。安全写入能够降低风险，但不能替代云盘版本历史和独立备份。

知识库位于云盘同步目录时，本地删除未必等于删除。当同一个知识库被多台设备或多个同步客户端同时看管（例如手机与电脑各自同步、或机器上残留了两个客户端进程），一端删除后另一端会把自己缓存的旧副本重新推上去，文件在几十秒内就会重新出现，且带有新的创建时间与旧的修改时间。

因此整理或合并笔记时：先确认只有一个同步客户端在运行，再在云盘网页端确认删除，最后检查其他设备不会回传本地缓存。本工具只做安全写入，不代替用户在云端执行删除，也无法阻止其他设备的回推。

## 报告问题

公开报告漏洞时，请不要附带真实知识库、聊天记录、邮件、私有路径、API密钥或队列文件。使用最小化的模拟数据复现。
